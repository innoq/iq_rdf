language: ruby
cache: bundler
bundler_args: "--without production --retry 3"
rvm:
  - 2.2
  - 2.3
  - 2.4
  - jruby-19mode

script: bundle exec rake test

branches:
  only:
    - master

notifications:
  email:
    - iqvoc@innoq.com
matrix:
  allow_failures:
    - rvm: ruby-head
